# Zenith.NET 代码风格与规范

> **基准**：`qian-o/Zenith.NET` 主分支 `master` @ `02d2fc8`（2026-08-05，"Add Skia InkCanvas integration and refine rendering backends (#30)"）。
> **分析方式**：对全仓库 256 个 C# 源文件（26,783 行）逐文件通读 + 全量脚本统计交叉验证，按子系统（核心抽象层 / DirectX12 / Metal / Vulkan / Extensions / Views / Experiments）归纳。
> **文档性质**：本文件是对仓库**既有事实风格**的完整总结，并融合**所有者显式偏好**（第 10 章，强制）。全文区分两类规则：从代码归纳的"事实风格"与所有者声明的"强制偏好"。新代码应当与本文件保持一致；当本文件与代码冲突时，以核心库 `sources/Zenith.NET/` 的现行写法为准。
> **证据约定**：文中 `路径:行号` 均相对于仓库根目录，代码摘录逐字取自该 commit 的源码。

---

## 目录

1. [总纲（一页速览）](#1-总纲一页速览)
2. [项目与工程配置](#2-项目与工程配置)
3. [文件与编码规范](#3-文件与编码规范)
4. [文件组织](#4-文件组织)
5. [命名规范](#5-命名规范)
6. [类型设计规范](#6-类型设计规范)
7. [成员排序](#7-成员排序)
8. [格式化细则](#8-格式化细则)
9. [语言特性使用规范（最新语法优先）](#9-语言特性使用规范最新语法优先)
10. [所有者显式偏好（强制）](#10-所有者显式偏好强制)
11. [RHI 后端专项规范](#11-rhi-后端专项规范)
12. [资源管理与 Dispose 模式](#12-资源管理与-dispose-模式)
13. [可空与"不防御"哲学](#13-可空与不防御哲学)
14. [注释与文档](#14-注释与文档)
15. [错误处理](#15-错误处理)
16. [平台差异与互操作](#16-平台差异与互操作)
17. [各子系统差异速查](#17-各子系统差异速查)
18. [.editorconfig（建议配置）](#18-editorconfig建议配置)
19. [规范适用范围与有意设计说明](#19-规范适用范围与有意设计说明)
20. [附录 A：快速自检清单](#附录-a快速自检清单提交前)
21. [附录 B：权威统计](#附录-b权威统计master--02d2fc8)

---

## 1. 总纲（一页速览）

| 维度 | 规则 |
|---|---|
| 编码 | UTF-8 **带 BOM**（255/256），LF 换行（256/256），4 空格缩进，零 Tab、零行尾空白 |
| 大括号 | **Allman**（开括号独占一行），零例外 |
| 命名空间 | **文件作用域** `namespace Xxx;`（248 个文件） |
| 文件 | 一文件一公共类型，文件名 = 类型名 |
| 成员顺序 | 字段 → 构造函数 → 属性 → 方法（`Destroy()`/抽象 `Impl` 收尾） |
| 空行 | 成员之间恰好一个空行；**方法体内用空行划分逻辑段** |
| 长行 | 优先单行；必须换行时**续行与首参数列对齐** |
| 类型推断 | **禁用 `var`**（全仓库 0 处）；显式类型 + target-typed `new()`（≈706 处） |
| Lambda | **优先 `static` 匿名方法**（`static item => ...`） |
| 模式匹配 | 优先 `is / is not / or / 关系模式 / switch 表达式`（≈364 处），兜底 `_ => default` |
| 构造 | 简单类用主构造函数（≈48 个类型）；`Desc` 结构体用对象初始化器 |
| 防御 | **零参数校验、零 Guard、零 Assert、几乎零异常**；无效操作静默返回 |
| 注释 | **零 XML 文档注释**（仅 2 个文件例外），注释极少且只用英文短句 |
| 资源 | 继承 `DisposableObject`，只重写 `protected override void Destroy()` |
| 诊断 | VS 2026 打开后 Error List **零错误、零警告、零信息** |

---

## 2. 项目与工程配置

以 `sources/Directory.Build.props` 为准，全部项目共享：

```xml
<PropertyGroup>
    <StandardTargetFramework>net10.0</StandardTargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
</PropertyGroup>
<ItemGroup>
    <PackageReference Include="Roslynator.Analyzers" ... />
</ItemGroup>
```

要点：

- **始终面向最新目标框架**（当前 `net10.0`，通过 `StandardTargetFramework` 统一定义，个别项目按需多目标）。
- **启用可空注解、隐式 using、unsafe 块**；构建期强制执行代码风格（`EnforceCodeStyleInBuild`）。
- 使用 **Roslynator 分析器**，代码必须对其零诊断。
- 包版本集中在 `Directory.Packages.props`（`ManagePackageVersionsCentrally`），`.csproj` 内只写 `PackageReference` 不写版本号。
- 核心库 `.csproj` 中带 `<NoWarn>IDE0130</NoWarn>`（命名空间与文件夹不匹配的提示被显式抑制）。
- NuGet 打包公共属性抽在 `NuGet.Packaging.props`，由需要打包的项目 `<Import>`。
- **CI**：`.github/workflows/continuous-integration.yml`，push 到 master 与任意 PR 触发；`windows-latest` + `macos-latest` 双平台矩阵，安装 .NET 10.0.x SDK 与 `maui` workload 后执行 `dotnet build -c Release`（构建必须零警告）。
- **换行归一化**：`.gitattributes` 仅一行 `* text=auto`。仓库当前**没有** `.editorconfig`（见第 18 章）。

---

## 3. 文件与编码规范

| 项 | 规范 | 依据（统计） |
|---|---|---|
| 字符编码 | UTF-8 **带 BOM** | 255/256 文件；唯一例外 `sources/Experiments/InkCanvas/Drawing/Stroke.cs`（演进差异，见 19.3） |
| 换行符 | LF（`.gitattributes` 设 `* text=auto` 归一化） | 256/256 文件 |
| 缩进 | 4 空格，禁止 Tab | 0 个文件含 Tab 缩进 |
| 行尾空白 | 不允许 | 0 处 |
| 文件头 | 无版权头、无 `<auto-generated/>`（仿生成文件除外，见 19.1） | — |
| 文件结尾 | 以单个末尾换行收尾 | 仅 4 个文件例外：`Experiments/FluidTank/Program.cs`、`Experiments/InkCanvas/Program.cs`、`Experiments/FluidTank/Helpers/CocoaHelper.cs`、`Experiments/InkCanvas/Helpers/CocoaHelper.cs`（演进差异，新文件必须末尾换行，见 19.3） |

---

## 4. 文件组织

### 4.1 一文件一类型

- 每个文件**恰好一个公共/顶层类型**，文件名与类型名严格一致（`Buffer.cs` → `Buffer`、`VKCommandBuffer.cs` → `VKCommandBuffer`）。
- 仅有的合法例外：
  - **`Usings.cs`**：后端项目集中存放 `global using` 别名（见 4.3）。
  - **`Extensions.cs`**：每个项目对外/对内的扩展成员集中在一个同名静态类中（C# 14 `extension` 块）。
  - **辅助类型内聚在宿主文件**：public 嵌套类型放宿主类**开头**（`ZenithMarshal.Scope`，`sources/Zenith.NET/ZenithMarshal.cs:8`）；私有嵌套类型放宿主类**末尾**（`CommandQueue.Submitted`、`Uploader.Lease`/`Downloader.Lease`）；示例项目把渲染器私有的常量缓冲布局做成**文件尾 `file struct`**（全库 10 处，见 6.5）。
- 仿生成风格的互操作文件（`ExtDescriptorHeap.cs`）允许一文件多类型，并以 `// <auto-generated/>` + `#pragma warning disable` 开头（见 19.1）。

### 4.2 using 布局

- using 一律放在文件顶部、namespace 之外。
- 排序：**`System.*` 在前，其余按字母序**（System 因字母序天然靠前，即纯字母序混排）。
- 依赖 `ImplicitUsings`，不重复写 `System`、`System.Collections.Generic` 等基础 using；同项目/父命名空间的类型**不写 using**（核心库 112 个文件中仅 8 个文件有 using）。
- 不引入未使用的 using。

实证（`sources/Zenith.NET.Vulkan/Extensions.cs:1-6`）：

```csharp
using System.Diagnostics;
using System.Runtime.CompilerServices;
using Silk.NET.Vulkan;

namespace Zenith.NET.Vulkan;
```

### 4.3 命名冲突：一律用别名，不用全限定名内嵌

- **后端项目**：冲突类型集中在 `Usings.cs` 定义全局别名，前缀 = 后端名小驼峰（`Dx*` / `Mtl*` / `Vk*`），按字母序排列。实证（`sources/Zenith.NET.DirectX12/Usings.cs:1-8`）：

```csharp
global using DxBlendOp = Silk.NET.Direct3D12.BlendOp;
global using DxCullMode = Silk.NET.Direct3D12.CullMode;
global using DxFillMode = Silk.NET.Direct3D12.FillMode;
global using DxHeapDesc = Silk.NET.Direct3D12.HeapDesc;
global using DxHeapType = Silk.NET.Direct3D12.HeapType;
global using DxMessageSeverity = Silk.NET.Direct3D12.MessageSeverity;
global using DxQueryHeapDesc = Silk.NET.Direct3D12.QueryHeapDesc;
global using DxQueryType = Silk.NET.Direct3D12.QueryType;
```

- **局部冲突**：文件内 using 别名解决，如 `using Buffer = Zenith.NET.Buffer;`、`using Feature = Silk.NET.Direct3D12.Feature;`、`using HexaImGui = Hexa.NET.ImGui.ImGui;`。

### 4.4 namespace

- 一律**文件作用域**声明，与项目名一致：`namespace Zenith.NET.Vulkan;`。全仓库 248 个文件为文件作用域。
- 无 namespace 的 7 个文件：4 个示例 `Program.cs` 顶层语句（其 `using Xxx;` 后直接 `App.Run();`，如 `sources/Experiments/CornellBox/Program.cs` 全文）+ 3 个后端 `Usings.cs`。（248 文件作用域 + 7 无 namespace + 1 块作用域 = 256）
- 唯一块作用域例外：仿 Silk 生成风格的 `ExtDescriptorHeap.cs`，含 `Silk.NET.Vulkan` 与 `Silk.NET.Vulkan.Extensions.EXT` 两个块作用域命名空间（见 19.1）。
- 示例项目按目录划分子命名空间（`CornellBox.Handlers` 对应 `Handlers/`）。

---

## 5. 命名规范

### 5.1 总表

| 元素 | 大小写 | 示例 |
|---|---|---|
| 类型（class/struct/enum/delegate） | PascalCase | `GraphicsContext`、`ResourceHandle` |
| 接口 | `I` + PascalCase | `IZenithView`、`IImGuiPlatformBindings` |
| 方法、属性、事件 | PascalCase | `BeginRenderPass`、`IsDisposed`、`UpdateRequested` |
| **public 字段** | PascalCase | `public VkBuffer Buffer;`、`public ComPtr<ID3D12Device10> Device;` |
| **private 实例字段** | camelCase，**无任何前缀**（不用 `_`、`m_`） | `currentPipeline`、`recycled`、`todoViewports` |
| private static / static readonly 字段 | camelCase 或 PascalCase（两种并存，跟随所在文件惯例） | `mapper` / `LeaseLifetime` |
| 常量（const） | PascalCase | `DefaultHeapAlignment`、`ExtensionName`、`Shader4ComponentMapping` |
| 方法内局部常量 | PascalCase 为主（部分文件 camelCase） | `RowPitchAlignment = 256` |
| 参数、局部变量 | camelCase | `sizeInBytes`、`createInfo`、`scope` |
| 枚举成员 | PascalCase，成员之间空行 | `DirectX12,` ⏎⏎ `Metal,` |
| 类型参数 | `T` / `T` + 语义 | `TChain`、`TNext` |
| 弃元 | `_` | `using Lock.Scope _ = @lock.EnterScope();`（`sources/Zenith.NET/CommandQueue.cs:17`） |
| lambda 弃元 | `_` / `(_, _)` | `Loaded += async (_, _) => await scheduler.StartAsync();`（`sources/Views/Zenith.NET.Views.WPF/ZenithView.cs:28`） |

### 5.2 专项约定

- **缩写词**：图形 API 与厂商缩写保持原样/全大写——`VK`/`DX`/`MTL`（类前缀）、`D3D`、`SK`、`UI`、`UTF8`、`MSAA`、`UNorm`、`SRgb`、`BC6HUFloat`；两个字母全大写（`MSAA`），三个及以上普通缩写首字母大写（`Fov`、`Dpi`、`Aabbs`）；枚举中 `x` 小写（`ASTC4x4`）。
- **单位后缀统一 `InBytes`**：`SizeInBytes`、`StrideInBytes`、`OffsetInBytes`、`RowStrideInBytes`。
- **后端实现钩子统一 `Impl` 后缀**：`CreateBufferImpl`、`BeginRenderPassImpl`、`SubmitImpl`（`protected abstract`，集中放类末尾）。
- **类型转换函数以后端名命名并全部重载**：`VKFormats.Vulkan(...)`、`DXFormats.DirectX12(...)`、`MTLFormats.Metal(...)`；配套的强转扩展方法同名：`texture.Vulkan()`、`texture.DirectX12()`、`texture.Metal()`。
- **关键字冲突用 `@` 转义，不改名**：`private readonly Lock @lock = new();`（`sources/Zenith.NET/CommandQueue.cs:5`）、`using ManualResetEventSlim @event = new(false);`（`sources/Views/Zenith.NET.Views/FrameScheduler.cs:83`）。
- **延迟应用的状态字段用 `todo` 前缀**（Metal 后端惯例，仅 `MTLCommandBuffer` 7 个字段，精确清单见 11.7）：`todoGraphicsPipeline`、`todoViewports` 等。
- 事件命名 `XxxRequested`（Views 层 `UpdateRequested`/`RenderRequested`）或语义名词（核心库唯一事件 `GraphicsContext.ValidationMessage`，`sources/Zenith.NET/GraphicsContext.cs:42`）；EventArgs 以 `EventArgs` 后缀，并用**主构造函数 + 只读属性初始化器**实现（`sources/Zenith.NET/ValidationMessageEventArgs.cs` 全文）：

```csharp
public class ValidationMessageEventArgs(MessageSeverity severity, string message) : EventArgs
{
    public MessageSeverity Severity { get; } = severity;

    public string Message { get; } = message;

    public DateTimeOffset Timestamp { get; } = DateTimeOffset.UtcNow;
}
```

  事件处理器命名 `OnXxx`。
- 布尔字段/变量直接用形容词或分词，不加 `is/has` 前缀：`pictureDirty`、`erasePending`、`CanTransition`。
- **事件参数命名一律用语义名**（`mouse`、`button`、`scanCode`、`position`）。历史遗留的 `arg1/arg2/arg3` 仅存在于 CornellBox 的 `CameraHandler`（演进差异，见 19.3），新代码不得模仿。

---

## 6. 类型设计规范

### 6.1 抽象靠 `abstract class`，不用 interface

核心抽象层**没有任何 interface**（核心库 112 个文件 0 接口）。全仓库仅 4 个接口声明、3 种：`IZenithView`、`IImGuiPlatformBindings`、`ISwapChainPanelNative`（后者在 Maui/WinUI 各有一份逐字副本）——都是 Views/Extensions 层的回调/互操作契约。GPU 资源一律抽象类 + 主构造函数，实证（`sources/Zenith.NET/Buffer.cs`）：

```csharp
public abstract class Buffer(GraphicsContext context, BufferDesc desc) : GraphicsResource(context)
```

- 抽象基类几乎全部使用**主构造函数**（全库主构造函数类型 ≈48 个），基类链直接 `: Base(context, desc)`。
- 唯一例外：`GraphicsContext` 是核心库**唯一使用普通构造函数的抽象类**，因为构造体需要调用 `Initialize` 虚方法并接收 5 个 `out` 参数回写属性（`sources/Zenith.NET/GraphicsContext.cs:5-23`）：

```csharp
protected GraphicsContext(GraphicsApi graphicsApi, bool useValidationLayer)
{
    GraphicsApi = graphicsApi;

    Initialize(useValidationLayer,
               out Capabilities capabilities,
               out CommandQueue graphicsQueue,
               out CommandQueue computeQueue,
               out CommandQueue transferQueue,
               out ValidationLayer? validationLayer);

    Capabilities = capabilities;
    GraphicsQueue = graphicsQueue;
    ...
}
```

- **空中间层允许无体主构造类**：`Pipeline` 仅作为类型层级节点存在，整类一行分号收尾（`sources/Zenith.NET/Pipeline.cs:3`）：

```csharp
public abstract class Pipeline(GraphicsContext context) : GraphicsResource(context);
```

- 不使用 `sealed`（全仓库仅 1 处，在 `ExtDescriptorHeap.cs` 的仿生成代码 `GeneratedVTable` 内）、不使用 `record`（0 处）、不使用 `partial`（平台拆分除外）。
- **私有字段与主构造参数同名遮蔽**：资源类把主构造参数存进同名字段，如 `private BufferDesc desc = desc;`（`sources/Zenith.NET/Buffer.cs:5`）。Buffer、BufferView、Texture、TextureView、Sampler、Shader、Heap、QueryHeap、SwapChain、GraphicsPipeline、ComputePipeline、MeshShadingPipeline、Bottom/TopLevelAccelerationStructure 等 11 个核心类（14 处声明）都是这个写法——这是规范而非笔误。
- 配套的 Desc 暴露为只读引用属性：`public ref readonly BufferDesc Desc => ref desc;`（`sources/Zenith.NET/Buffer.cs:7`）。

### 6.2 Desc / 数据载体 = 可变公共字段 struct + 静态工厂

```csharp
public struct GraphicsPipelineDesc
{
    public Shader VertexShader;

    public Shader FragmentShader;
    ...
}
```

- 纯数据、无构造函数、无校验、无属性——**public 可变字段**，字段之间空行。
- 配套静态工厂方法作为用户侧主要构造入口，工厂体固定为 `return new() { ... };` 全字段显式赋值：

```csharp
public static SamplerDesc LinearWrap() => new() { AddressModeU = AddressMode.Wrap, ... };
// 或块体
public static TextureDesc Texture2D(...) { return new() { Type = ..., ... }; }
```

### 6.3 小值类型 = `readonly struct` + 主构造函数 + 公开只读字段

```csharp
public readonly struct ResourceHandle(uint x, uint y)
{
    public readonly uint X = x;

    public readonly uint Y = y;
}
```

### 6.4 internal 类允许 public 字段

**类若是 `internal`，其需要被程序集内其他类访问的值类型/句柄成员直接声明为 `public` 字段**，不封装成属性：

```csharp
internal unsafe class DXGraphicsContext(bool useValidationLayer) : GraphicsContext(...)
{
    public ComPtr<ID3D12Device10> Device;

    public ComPtr<ID3D12RootSignature> RootSignature;
    ...
}
```

理由：程序集边界即封装边界；字段语义便于 `ComPtr.GetAddressOf()` 取址与跨类直读，省去包装成本。这是规范而非疏漏（同时是所有者显式偏好第 7 条）。

### 6.5 GPU 常量缓冲镜像 = 文件尾 `file struct`

示例/渲染器中与着色器常量缓冲对应的结构体用 `file struct` 放文件末尾（全库 10 处：9 个示例常量缓冲 + `ImGuiRenderer.cs:507` 的 `file struct Constants`），配显式内存布局：

```csharp
[StructLayout(LayoutKind.Explicit, Size = 176)]
file struct SceneConstants
{
    [FieldOffset(0)]
    public Matrix4x4 View;
    ...
}
```

实证：`sources/Experiments/FluidTank/FluidTankRenderer.cs:660`（`SceneConstants`，同文件另有 `SurfaceConstants`/`BlurConstants`/`CompositeConstants`/`ReflectionConstants`/`GlassConstants` 共 6 个）、`sources/Experiments/CornellBox/Renderers/PathTracingRenderer.cs:235`、`sources/Experiments/FluidTank/FluidSimulation.cs:272`。共享的顶点/材质结构用 `internal struct`，放在 Geometry helper 文件尾部。

---

## 7. 成员排序

固定骨架（全仓库高度一致）：

1. **字段**：`const` → `static readonly` → `readonly` 实例字段 → 可变私有字段；后端类把 **public 句柄字段**置顶（private 字段或在其前、或紧随其后成组）。
2. **构造函数**（多个重载相邻连排；静态构造函数紧随静态字段）。
3. **`public new XxxGraphicsContext Context` 收窄属性**（仅后端类中需要反复访问 Context 的类才有，紧跟构造函数，详见 11.1）。
4. **属性**（get-only → 自动属性 → override 属性）。
5. **方法**（public → internal → protected override；重载相邻；按生命周期/调用顺序排布，如 `Update → Render → Resize → Dispose`）。
6. **`SetResourceName` → `Destroy()`**：`Destroy()` 一般为最后一个 protected 成员。
7. **private 辅助方法 / static 工具**沉底。
8. **核心抽象层**：`protected abstract XxxImpl(...)` 列表集中放类末尾。
9. **嵌套类型**：public 嵌套类型放类**开头**（`ZenithMarshal.Scope`，`sources/Zenith.NET/ZenithMarshal.cs:8`）；private 嵌套类型放类/文件**末尾**（`CommandQueue.Submitted`、`Uploader.Lease`、`Downloader.Lease`；`MTLCommandBuffer` 尾部 4 个主构造形态的 private 嵌套 struct：`IndexBinding`、`VisibilityKey`、`VisibilityBinding`、`ResolveTimestamp`，`sources/Zenith.NET.Metal/MTLCommandBuffer.cs:665-697`）。
10. 显式接口实现集中垫底（Views 层惯例）。

后端类成员布局通则（实证归纳）：**字段（public 原生句柄在前）→ 构造函数 → new Context 收窄属性 → 属性 → public override → protected override Impl（按基类抽象成员声明顺序）→ SetResourceName → Destroy 收尾 → private 辅助/static 工具沉底**。

已确认的特例（有意设计，见 19.2）：

- `sources/Zenith.NET/CommandQueue.cs:71-77`：`protected abstract` 列表在 `Destroy()` **之前**。
- `sources/Zenith.NET/CommandBuffer.cs:439-475`：private `TryGetCurrentPipeline` 两个重载位于 `Destroy()` 与 abstract 块**之间**。
- `sources/Zenith.NET/GraphicsContext.cs:169`：`internal void OnValidationMessage` 位于 abstract 列表**之后**。
- `sources/Zenith.NET.Metal/MTLCommandBuffer.cs`：`Destroy()`（:539）**不是最后**，其后还有 private 编码器辅助方法（`BeginRenderEncoding` 等 :549-663）与尾部嵌套 struct。

修饰符习惯：`public > internal > protected > private`，非静态 > 静态；同组内再按功能/业务逻辑分组。

---

## 8. 格式化细则

### 8.1 Allman 大括号，零例外

类型、方法、属性、if/for/switch/try 的开括号**全部独占一行**（`sources/Zenith.NET/DisposableObject.cs:15-18`）：

```csharp
if (Interlocked.Exchange(ref isDisposed, 1) is not 0)
{
    return;
}
```

- **switch 语句**的 case 体用大括号块包裹、`break` 另起一行：

```csharp
case GraphicsApi.Metal:
    {
        nint device = context.GetNativeObject(NativeObjectType.MTLDevice);
        ...
    }
    break;
```

### 8.2 空行分组（方法内强制）

- 成员之间**恰好一个空行**；struct 的字段之间也空行；枚举成员之间空行。
- **方法体内用空行划分逻辑段**，段内不空行。实证（`sources/Zenith.NET.Metal/MTLCommandBuffer.cs:37-46`）：

```csharp
CommandBuffer = NSAutorelease.Own(context.Device.MakeCommandBuffer);

MTL4ArgumentTableDescriptor descriptor = new() { ... };

ArgumentTable = context.Device.MakeArgumentTable(descriptor, out NSError error);
error.Success();
```

- 大段配置逻辑用 **`// 标题注释` + 裸大括号作用域** 分组（后端管线/上下文初始化的签名式写法）。实证（`sources/Zenith.NET.DirectX12/DXGraphicsPipeline.cs:23-25, 50-52`）：

```csharp
// InputLayouts
{
    List<InputElementDesc> inputElementDescs = [];
    ...
}
// AttachmentFormats
{
    ...
}
```

### 8.3 长行：单行优先，换行则对齐

- **一行过长的代码尽量不换行**——仓库中存在大量 130~250+ 字符的单行（链式调用、三元、ImGui 调用），刻意保持单行。
- 实在太长必须换行时，**续行与首参数列对齐**（不是悬挂 +4 缩进）。实证（`sources/Zenith.NET/GraphicsContext.cs:8-13`、`sources/Zenith.NET.DirectX12/DXBuffer.cs:19-27`）：

```csharp
Initialize(useValidationLayer,
           out Capabilities capabilities,
           out CommandQueue graphicsQueue,
           out CommandQueue computeQueue,
           out CommandQueue transferQueue,
           out ValidationLayer? validationLayer);

context.Device.CreateCommittedResource3(&heapProperties,
                                        HeapFlags.None,
                                        &resourceDesc,
                                        BarrierLayout.Undefined,
                                        default(ClearValue*),
                                        ...
```

- 方法**声明**的多参数换行同样对齐到首参数列：

```csharp
public static GraphicsPipeline CreateGraphicsPipeline(string file,
                                                      string vertexEntryPoint,
                                                      string fragmentEntryPoint,
                                                      ...
                                                      PrimitiveTopology primitiveTopology = PrimitiveTopology.TriangleList)
```

- 链式调用（含 LINQ 链）**不换行**，写在一行。
- 三元表达式**不拆行**（哪怕超过 130 列）。
- 多返回值元组用 `return ( ... )` 竖排：

```csharp
return
(
    pixelFormat switch { ... },
    pixelFormat switch { ... }
);
```

### 8.4 初始化器与集合

- 对象初始化器：`new()` 后 `{` Allman 换行，每属性一行，**无尾逗号**；单属性初始化器可写单行 `new() { Count = 1 }`。
- 集合表达式元素少时单行 `[colorAttachment]`；元素多时每元素一行。
- 位或枚举拼接在初始化器内换行并额外缩进对齐。

### 8.5 switch 表达式（映射表）排版

- 每分支一行，`_ => default` 兜底收尾；大表按语义分组、组间空行。实证（`sources/Zenith.NET.Vulkan/VKBuffer.cs:153-162`）：

```csharp
return type switch
{
    NativeObjectType.VulkanBuffer => (nint)Buffer.Handle,
    NativeObjectType.VulkanDeviceAddress => (nint)DeviceAddress,
    NativeObjectType.VulkanDeviceMemory => (nint)Allocation.DeviceMemory.Handle,
    NativeObjectType.VulkanDeviceMemoryOffset => (nint)Allocation.OffsetInBytes,
    _ => default
};
```

- `or` 模式成组：候选项可同行（`A or B => x`），过长时每个候选独占一行。
- 区间关系模式可折叠成组臂。实证（`sources/Experiments/FluidTank/Handlers/ImGuiHandler.cs:140`）：

```csharp
>= Key.Number0 and <= Key.Number9 => ImGuiKey.Key0 + (key - Key.Number0),
```

---

## 9. 语言特性使用规范（最新语法优先）

**总原则：喜欢并优先使用最新的语法和关键字（所有者显式偏好第 8 条）。** 当前基线为 C# 13/14 + .NET 10，以下特性为**标准用法**：

| 特性 | 规范用法 | 实证 |
|---|---|---|
| **target-typed `new()`** | 类型在左侧显式写出，右侧一律 `new()` / `new(...)`，禁止重复类型名 | 全仓库 ≈706 处，`var` 0 处 |
| **集合表达式** | `[]`、`[item]`、`[.. spread]`、stackalloc 集合字面量 | `List<InputElementDesc> inputElementDescs = [];`（`DXGraphicsPipeline.cs:25`） |
| **主构造函数** | 构造函数体为空或极简时使用；参数直接被字段/属性初始化器捕获 | ≈48 个类型；`DXGraphicsContext(bool useValidationLayer) : GraphicsContext(...)` |
| **模式匹配** | `is null` / `is not null` / `is not 0` / `is 0` / `or` / 关系模式 `is < 64` / 声明模式 `is TPipeline typed` / 元组模式 | ≈364 处（其中 `is not` 185）；**代替 `!=` / `==` 常量比较** |
| **switch 表达式** | 一切映射/查表的唯一写法，`_ => default` 兜底 | ≈120+ 处 |
| **`field` 关键字（C# 14）** | 属性 setter 校验、懒加载：`=> field ??= new(...)` | 仅 4 个文件：`GraphicsResource.cs:10-12`（Name setter 校验）、`DXGraphicsContext.cs:33-39`（4 个 `field ??=` 懒加载描述符堆）、`VKGraphicsContext.cs`（`field ??=` ResourceHeap）、`Experiments/FluidTank/FluidSimulation.cs`（`set => field = Math.Clamp(...)`） |
| **`extension` 成员（C# 14）** | 扩展方法不用经典 `this` 参数，统一写 `extension(T x) { ... }` 块；含静态扩展工厂 | 全库 71 处 extension 块：三后端 `Extensions.cs` + `ExtDescriptorHeap.cs` + ImageSharp/Skia/Maui 的 Extensions |
| **`Lock` / `Lock.Scope`（.NET 9+）** | 锁对象用 `Lock` 类型；`using Lock.Scope _ = @lock.EnterScope();` | `sources/Zenith.NET/CommandQueue.cs:5,17,44,59` 等 |
| **static 匿名方法** | 不捕获闭包的 lambda 一律加 `static` | 全库 16 处：核心 `Uploader.cs:20,66`/`Downloader.cs:20,66`、`VKCapabilities`/`VKGraphicsContext`/`VKGraphicsPipeline`/`VKMeshShadingPipeline`、Maui Extensions、`ImGuiRenderer` 的 `Comparer.Create`、3 个示例 App 的 `ValidationMessage` 挂接 |
| **表达式体成员** | **属性**用 `=>`（`public CommandQueue Queue => queue;`）；**方法不用**——即使单行方法也写块体 + `return` | 全仓库一致 |
| `default` 字面量 | switch 兜底、allocator 参数、空指针实参一律优先 `default`；仅重载无法推断时写 `default(T*)` 限定（如 `ComPtr<T>` 与 `T*` 重载并存） | `CreateFence(..., default, out Fence)`；`default(ClearValue*)`（`DXBuffer.cs:23`） |
| `nint` / `nuint` | 一切指针/句柄/地址的整数表达 | `public nint Pointer;` |
| `Span<T>` / `stackalloc` | 指针拷贝惯用法 `new ReadOnlySpan<byte>((void*)src, n).CopyTo(...)`；小数组 `stackalloc T[n]` + for 填充 | Buffer、VKCommandQueue |
| 命名元组 | 多值返回 `(PipelineStageFlags2 Stage, AccessFlags2 Access)`；解构含弃元 `(_, _, uint w, uint h)` | 各 Formats 映射 |
| `with` 表达式 | struct/配置对象改少数字段 | `extent with { Depth = 1 }` |
| 从末尾索引 `^1` | `Elements[^1] = element;`（两段拼接） | 少量使用 |
| `params ReadOnlySpan<T>` | 变长只读参数 | **全库仅 1 处**：`public TimelineValue Submit(params ReadOnlySpan<TimelineValue> waits)`（`sources/Zenith.NET/CommandBuffer.cs:11`） |
| raw string literal | 嵌入着色器源码 | ImGuiRenderer 的统一 SLang 源码 `"""..."""`（见 9.1） |
| `file` 类型 | 文件私有辅助类型 | 10 处 `file struct`（见 6.5） |
| 源生成互操作 | `[LibraryImport]` + `partial` 方法、`[GeneratedComInterface]`；**禁止手写 `[DllImport]`**（0 处） | 22 处 `[LibraryImport]`、2 处 `[GeneratedComInterface]`（见第 16 章） |
| 顶层语句 | 仅 `Program.cs`（示例入口可两行：`using Xxx;` + `App.Run();`） | `sources/Experiments/CornellBox/Program.cs` 全文即如此 |

### 9.1 ImGuiRenderer 的着色器：一份统一 SLang 源码

`ImGuiRenderer` **不是嵌入 HLSL**，而是一份统一的 SLang 风格着色器源码（`sources/Extensions/Zenith.NET.Extensions.ImGui/ImGuiRenderer.cs:10` 起）：

```csharp
public const string Source = """
...
""";
```

构造时按当前图形 API 编译，色彩空间变体靠**运行时字符串替换**翻转着色器内的预处理开关（`ImGuiRenderer.cs:81-86`）：

```csharp
string source = Source.Replace("#if 0", $"#if {(colorSpace is ImGuiColorSpace.Legacy ? 0 : 1)}");

using Shader vertex = context.CreateShader(ZenithCompiler.CompileFromSource(context.GraphicsApi, source, "VSMain"));
using Shader fragment = context.CreateShader(ZenithCompiler.CompileFromSource(context.GraphicsApi, source, "FSMain"));
```

注意：raw string 内的 `#if 0`（`ImGuiRenderer.cs:54`）是**着色器预处理指令，不是 C# 级 `#if`**；统计 C# 条件编译时不要把它算进去（C# 级 `#if` 全库仅 3 个文件，见第 16 章）。

**明确不使用的特性**：`var`（0 处）、`record`（0 处）、`sealed`（仅 1 处仿生成代码）、`in`/`scoped` 参数、范围 `..`（集合展开除外）、表达式体方法。

---

## 10. 所有者显式偏好（强制）

以下偏好为所有者显式声明，与仓库现状完全一致，列为**强制规则**：

1. **优先 `new()` 简化**：任何目标类型明确的 `new Xxx(...)` 都必须写成 `new(...)`；对象初始化器写 `new() { ... }`。
   - 配套查找正则（简化属性赋值时定位最后一个属性后的逗号）：`,+[\s]+[` + `^\S\r\n]+};`（两段拼接即完整正则）
2. **RHI 后端优先使用不安全（unsafe）方法**，减少参数传递：`unsafe` 直接标在**类级别**（`internal unsafe class VKCommandBuffer`），不做逐方法标注；指针（`&createInfo`、`(void**)`、`nint`）直传，而不是层层包装安全重载。三后端的类级 unsafe 粒度不同（见 11.1），具体以当前代码风格为准。
3. **方法内代码用空行分组**（见 8.2）。
4. **长行不换行优先；换行则对齐**（见 8.3）。
5. **VS 2026 零诊断**：任何人用 VS 2026 打开项目，Error List 不得有任何错误、警告、信息（见第 2、18 章）。
6. **优先静态匿名方法**：不捕获外部变量的 lambda/匿名方法必须加 `static`：`static item => item.Property`、`static (sender, args) => ...`。
7. **既有 Code Standard**（原文保留，全部有效）：
   - Prefer using `(static item => item.Property)` Lambda expressions.
   - Prefer using `new()` syntax sugar.
   - Prefer using pattern matching. Such as `is, is not, =>`.
   - If a class is `internal` and has value type parameters that need to be accessed externally, allow `public Type FieldName`.
   - Class members order: Fields, Constructors, Properties, Methods.
   - Modifiers: public > internal > protected > private, non-static > static.
   - The remaining modifiers are sorted by function or business logic.
8. **使用最新语法和关键字**（见第 9 章）。
9. **禁用 `var`**：局部变量类型一律显式写出（`var` 全仓库 0 处，保持为 0）。
10. **反过度封装、反防御性编程、反异常处理**（见第 6、13、15 章）：不写 Guard 子句、不写 `ArgumentNullException`、不写 `Debug.Assert`、不写 try/catch 包装、不为 internal 成员写属性封装。

---

## 11. RHI 后端专项规范

适用于 `Zenith.NET.DirectX12` / `Zenith.NET.Metal` / `Zenith.NET.Vulkan` 三个后端项目。

### 11.1 类骨架（三后端同构）

```csharp
internal unsafe class VKBuffer : Buffer
{
    public VkBuffer Buffer;                       // ① 原生句柄：public 可变字段

    public VKBuffer(VKGraphicsContext context, BufferDesc desc) : base(context, desc) { ... }

    public new VKGraphicsContext Context => (VKGraphicsContext)base.Context;   // ② 协变收窄

    public override nint GetNativeObject(NativeObjectType type) => type switch { ... };

    protected override void SetResourceName(string name) { ... }

    protected override void Destroy() { ... }     // ③ 一般收尾（Metal 特例见第 7 章）
}
```

- 类命名：`VK`/`DX`/`MTL` 前缀；继承核心抽象基类。
- **类级 `unsafe` 按指针需要标注，三后端粒度不同**（所有者偏好第 2 条的落地形态）：
  - **DirectX12**：几乎全部 `internal unsafe class`（27 个类文件中 21 个）；
  - **Vulkan**：几乎全部 `internal unsafe class`（32 个类文件中 22 个），**例外：`VKSampler` 是唯一非 unsafe 的资源类**——`internal class VKSampler : Sampler`（`sources/Zenith.NET.Vulkan/VKSampler.cs:5`）；
  - **Metal**：只有碰指针的 **4 个类**是 unsafe——`MTLCommandBuffer`、`MTLQueryHeap`、`MTLBottomLevelAccelerationStructure`、`MTLTopLevelAccelerationStructure`；其余均为普通 `internal class`。
- **`public new XxxGraphicsContext Context` 收窄属性并非每个后端类都有**：只在需要反复访问 Context 的类中出现（VK 15 个类、DX 7 个类、MTL 8 个类），位置固定紧随构造函数。不需要反复访问的类直接用基类 `Context` 或自行持有（如 `VKFence` 用 `Context = context;` 普通属性赋值，见 19.2）。`VKTimeline`/`DXTimeline` 另有同形态的 `public new XxxCommandQueue Queue` 收窄（`sources/Zenith.NET.Vulkan/VKTimeline.cs:20`、`sources/Zenith.NET.DirectX12/DXTimeline.cs:19`）：

```csharp
public new VKCommandQueue Queue => (VKCommandQueue)base.Queue;
```

- 后端转换集中在一个 `internal static class XxxFormats`，方法名一律重载为后端名 `Vulkan(...)` / `DirectX12(...)` / `Metal(...)`。
- 强转扩展同名：`texture.Vulkan()`，定义在各项目 `Extensions.cs` 的 `extension` 块中。
- **`GetNativeObject` 有两种合法形态**，按类是否有原生对象可暴露选择：
  1. 支持原生导出的类：switch 表达式 + `_ => default` 兜底（实证见 8.5，`VKBuffer.cs:153-162`）；
  2. 没有任何原生对象需要暴露的类：直接 `return 0;`（DX 11 个类、VK 10 个类、MTL 12 个类；如 `sources/Zenith.NET.DirectX12/DXGraphicsPipeline.cs:148-151`、`VKSampler.cs`、`DXShader.cs`）。
  两种都是规范写法，无需统一。

### 11.2 错误检查：`.Success()` 软失败（三后端三种形态）

全库 143 处 `.Success()`（Vulkan 83、DirectX12 52、Metal 8）。**只 Debug 输出、不抛异常**。三后端的扩展签名不同，调用形态随之不同：

- **Vulkan**：`extension(Result)`，行尾链式调用：

```csharp
context.Vk.CreateCommandPool(context.Device, &createInfo, default, out CommandPool).Success();
```

- **DirectX12**：`extension(int result)`（HRESULT），同样行尾链式；另有 `IsSuccess()` 辅助（`sources/Zenith.NET.DirectX12/Extensions.cs:25`），**仅 `DXGraphicsContext` 使用 2 处**（`:59`、`:68`，用于需要按成败走分支的初始化）：

```csharp
if (useValidationLayer && D3D12.GetDebugInterface(SilkMarshal.GuidPtrOf<ID3D12Debug>(), (void**)debug.GetAddressOf()).IsSuccess())
```

- **Metal**：Metal.NET 的 `Make*` 工厂方法用 **`out NSError error` 单独成行接收**，再对 `error` 调 `error.Success()`（返回值已被赋值，无法链尾）；`Success()` 内部还负责 `error.Dispose()`。实证（`sources/Zenith.NET.Metal/MTLCommandBuffer.cs:45-46`）：

```csharp
ArgumentTable = context.Device.MakeArgumentTable(descriptor, out NSError error);
error.Success();
```

可空扩展 API 用 `?.` 级联后仍可 `.Success()`：

```csharp
Context.DebugUtils?.SetDebugUtilsObjectName(Context.Device, &nameInfo).Success();
```

### 11.3 临时非托管内存：作用域分配器

构造/编码类方法开头固定（实证 `sources/Zenith.NET.Vulkan/VKGraphicsContext.cs:152`、`sources/Zenith.NET.DirectX12/DXGraphicsPipeline.cs:12`）：

```csharp
using ZenithMarshal.Scope scope = new();
```

配 `ZenithMarshal.Allocate<T>(scope, count)` / `AllocateAndFill(scope, [...])` / `StringToPointer(scope, ..., StringEncoding.UTF8)`，方法结束整批释放。小数组优先 `stackalloc` + for 循环逐个 `new() { ... }` 填充。

### 11.4 双段枚举查询定式（Vulkan）

实证（`sources/Zenith.NET.Vulkan/VKSwapChain.cs:168-173`）：

```csharp
uint presentModeCount = 0;
Context.Surface?.GetPhysicalDeviceSurfacePresentModes(Context.PhysicalDevice, Surface, &presentModeCount, null).Success();

PresentModeKHR* presentModes = (PresentModeKHR*)ZenithMarshal.Allocate<PresentModeKHR>(scope, presentModeCount);
Context.Surface?.GetPhysicalDeviceSurfacePresentModes(Context.PhysicalDevice, Surface, &presentModeCount, presentModes).Success();
```

注意 `VKSwapChain.cs:170` 的空指针实参用了字面 `null`——这是与"优先 `default`"规则的**唯一偏差**（见 19.3），新代码此处应写 `default`。

### 11.5 P/Next 链定式（Vulkan）

实证（`sources/Zenith.NET.Vulkan/VKBuffer.cs:26, 39-40`）：

```csharp
requirements2.AddNext(out MemoryDedicatedRequirements dedicatedRequirements);
...
allocateInfo.AddNext(out MemoryDedicatedAllocateInfo dedicatedAllocateInfo);
dedicatedAllocateInfo.Buffer = Buffer;
```

结构体初始化首行恒为 `SType = StructureType.Xxx`；Silk 未收录的 EXT 类型用带括号的扩展 `StructureType.XxxExt()`；allocator 参数恒传 `default`。**已知的 `new()` 例外**：对"带可选参数构造函数的 struct"（如 `SamplerCreateInfo`）写**全类型名** `new SamplerCreateInfo() { ... }` 而非 `new()`（`sources/Zenith.NET.Vulkan/VKSampler.cs:11` 起）——`new()` 会触发可选参数构造重载的歧义/不同行为，此处保留全类型名是有意的。

### 11.6 Formats 映射表的三种形态

1. **枚举→枚举**：`return x switch { ... _ => default };`，大表按语义分组、组间空行（见 8.5）。
2. **flags 累积**：`T result = default;` + 一串带花括号的 `if (x.HasFlag(...)) { result |= ...; }` + `return result;`。
3. **多值返回**：命名元组 + 多个 switch 竖排（见 8.3）。

### 11.7 Metal 专项惯例

- **延迟应用状态 = `todo` 前缀字段（仅 `MTLCommandBuffer`，精确 7 个）**（`sources/Zenith.NET.Metal/MTLCommandBuffer.cs:26-32`）：

```csharp
private GraphicsPipeline? todoGraphicsPipeline;
private ComputePipeline? todoComputePipeline;
private MeshShadingPipeline? todoMeshShadingPipeline;
private Viewport[]? todoViewports;
private Scissor[]? todoScissors;
private Vector4? todoBlendConstant;
private uint? todoStencilReference;
```

  回放模板固定出现在 `BeginRenderEncoding`/`BeginComputeEncoding` 中（`:554-557`）：

```csharp
if (todoGraphicsPipeline is not null)
{
    SetPipeline(todoGraphicsPipeline);

    todoGraphicsPipeline = null;
}
```

  `ResetImpl` 负责统一清空全部 `todo*` 字段。

- **`NSAutorelease.Own`**：Metal 的 autoreleased 对象经 `NSAutorelease.Own(...)`（内部在 `NSAutoreleasePool` 作用域里 Retain）取得所有权；**方法组直接作委托传入**。全库 6 处调用：`MTLCommandBuffer.cs:37`（`MakeCommandBuffer`）、`:551`（`MakeRenderCommandEncoder`）、`:620`（`MakeComputeCommandEncoder`）、`MTLSwapChain.cs:58/66/102`（`MetalLayer.NextDrawable`）。实证：

```csharp
CommandBuffer = NSAutorelease.Own(context.Device.MakeCommandBuffer);
```

---

## 12. 资源管理与 Dispose 模式

### 12.1 库代码：DisposableObject 模板方法

一切 GPU 对象继承 `DisposableObject`（核心库唯一实现点，`sources/Zenith.NET/DisposableObject.cs` 全文）：

```csharp
public abstract class DisposableObject : IDisposable
{
    private volatile uint isDisposed;

    ~DisposableObject()
    {
        Dispose();
    }

    public bool IsDisposed => isDisposed is not 0;

    public void Dispose()
    {
        if (Interlocked.Exchange(ref isDisposed, 1) is not 0)
        {
            return;
        }

        Destroy();

        GC.SuppressFinalize(this);
    }

    protected abstract void Destroy();
}
```

派生类规则：

- **只重写 `protected override void Destroy()`**；绝不自己实现 `IDisposable.Dispose`、绝不写 `Dispose(bool)`、绝不写终结器（基类已带）。
- `Destroy()` 内手写释放清单，**大致逆序**，按语义空行分组；有基类资源时先 `base.Destroy();`。
- 有归属登记的资源先注销再释放：`Context.Unregister(Buffer);` → `View.Dispose();` → `Buffer.Dispose();`。
- 条件销毁用显式标志：`if (Allocation.OwnsResource) { ... }`。

### 12.2 借用式对象

`CommandBuffer` 与 `SwapChain.Drawable` 是**借用品**：录完即提交，**不保留、不 Dispose**（由队列回收池管理）。示例渲染主循环严格照此执行：**借来的 `CommandBuffer` 从不 Dispose**。FluidTank 一帧借两个 `CommandBuffer`（scene 一个 + 主渲染一个），用 `TimelineValue` 做帧间同步；上传路径同样借用后立即 `Submit().Wait()`（`sources/Experiments/FluidTank/FluidTankRenderer.cs:79-86`）：

```csharp
CommandBuffer uploadCommandBuffer = App.Context.TransferQueue.CommandBuffer();
sceneVertexBuffer = GraphicsHelper.LoadBuffer(uploadCommandBuffer, sceneVertices, BufferUsages.Vertex | BufferUsages.StorageReadOnly);
...
uploadCommandBuffer.Submit().Wait();
```

### 12.3 临时资源：using 声明（非 using 块）

```csharp
using Shader vertexShader = App.Context.CreateShader(...);
using ZenithMarshal.Scope scope = new();
using Lock.Scope _ = @lock.EnterScope();
```

- 一律用 **using 声明**（无大括号），作用域到方法尾。
- **Shader 对象在管线创建后即释放**（using 声明，不做字段持有）。
- 可空资源用 `?.Dispose()`。

### 12.4 示例代码的最简模式

示例项目不用基类，直接 `public void Dispose()` 逐个释放：无终结器、无 `Dispose(bool)`、无 `GC.SuppressFinalize`；按字段**逆序**释放；派生类末尾调 `base.Dispose()`。

---

## 13. 可空与"不防御"哲学

- `Nullable enable` 下，**注解稀疏但精确**：只在确实可空处标 `?`（`Pipeline? currentPipeline`、`ValidationLayer?`、`out ValidationLayer? validationLayer`）。
- **零参数校验**：全仓库 0 处 `ArgumentNullException`、0 处 Guard、0 处 `Debug.Assert`。
- 空安全三件套：
  1. **提前返回**：`if (x is null) { return; }` / `if (!TryGetCurrentPipeline(out ...)) { return; }`；
  2. **`?.` 链**：`surface?.Dispose();`；
  3. **静默降级**：switch 兜底 `_ => default` / `_ => 0` / `_ => []`。
- **`!` 容忍符的合法场景只有两种**：
  - 构造后赋值的字段/属性：`= null!;`（如 `private Texture sceneColor = null!;`）；
  - 框架/平台保证非空处：`window.Native!.Win32!.Value.Hwnd`。
- Try 模式失败分支可写 `pipeline = null!;`。
- **公开 API 完全不防御**——调用方契约由文档与命名表达，不靠运行时代码。

---

## 14. 注释与文档

- **零 XML 文档注释**：全仓库 256 个文件中仅 2 个文件有 `///`——`sources/Zenith.NET.DirectX12/PixHelpers.cs`（移植出处 URL）与 `sources/Zenith.NET.Vulkan/ExtDescriptorHeap.cs`（仿生成占位）。
- **行内注释极少**，且只有两种合法形式：
  1. **逻辑分块标题**：`// InputLayouts`、`// AttachmentFormats`——独立成行、英文、短、无句号，配裸 `{ }` 作用域（实证见 8.2）；
  2. **必要的"为什么"说明**：`// Ignore other exceptions to keep the loop running.`（`sources/Views/Zenith.NET.Views/FrameScheduler.cs:132`）。
- Views/Extensions 层同样极简：两层合计 0 处 throw、0 处 XML 注释，行注释仅 2 处。
- 注释**一律英文**，不写中文注释，不写行尾注释。
- **不写 TODO/FIXME/HACK**。
- 设计哲学：**命名即文档**——`SizeInBytes`、`OwnsResource`、`BeginRenderPass` 这类命名取代注释；公共 API 的文档由 docfx 文档站（`documents/`）承载，而非代码内注释。

---

## 15. 错误处理

- **原生调用失败**：`.Success()` 扩展 → Debug 输出软失败，不抛异常（刻意的容错设计，失败继续跑；三形态见 11.2）。
- **允许抛异常的场景只有"环境/能力不满足"**。全库 `throw` 恰好 8 处：
  - `NotSupportedException` × 4：`DXGraphicsContext.cs:70`、`MTLGraphicsContext.cs:49`、`VKGraphicsContext.cs:296`（三后端能力检查）+ `Experiments/PlatformDetection` 的 switch 兜底臂；
  - `PlatformNotSupportedException` × 4：3 个示例 App 的平台检查 + FluidTank 的 X11 检查。

  实证（`sources/Zenith.NET.Metal/MTLGraphicsContext.cs:47-50`）：

```csharp
if (!Device.SupportsFamily(MTLGPUFamily.Metal4))
{
    throw new NotSupportedException("This device does not support Metal 4.0 or higher.");
}
```

  消息一律为英文完整句。
- **不写 try/catch 防御包装**。全库 try/catch 仅 2 个文件，且都是明确语义的吸收：
  - `sources/Experiments/PlatformDetection/Program.cs`：`catch (Exception)` 探测式吸收（故意试探 API 是否可用）；
  - `sources/Views/Zenith.NET.Views/FrameScheduler.cs:126-133`：`catch (OperationCanceledException) { break; }` + 裸 `catch` 保活循环：

```csharp
catch (OperationCanceledException)
{
    break;
}
catch
{
    // Ignore other exceptions to keep the loop running.
```

- 无效操作**静默 return**，不抛 `InvalidOperationException`。

---

## 16. 平台差异与互操作

- **优先运行时分支，不用 `#if`**：`OperatingSystem.IsWindows()/IsMacOS()/IsAndroid()` 的 if-else 或三元。实证（`sources/Views/Zenith.NET.Views/ZenithViewHelper.cs:5`）：

```csharp
public static PixelFormat DrawableFormat { get; } = OperatingSystem.IsAndroid() ? PixelFormat.R8G8B8A8UNorm : PixelFormat.B8G8R8A8UNorm;
```

  平台 API 可用性用 `enabledExtensions.Contains(...) ? new(context) : null` 三元。
- **C# 级 `#if` 全库仅 3 个文件**，都属 UI 框架适配层：
  - `sources/Views/Zenith.NET.Views.Maui/ZenithViewHandler.cs`：`#if ANDROID/IOS/MACCATALYST/WINDOWS` 选择 using；
  - `sources/Views/Zenith.NET.Views.WinUI/ZenithView.WinUI.cs:1`：整文件 `#if WINDOWS`；
  - `sources/Views/Zenith.NET.Views.WinUI/ZenithView.Uno.cs:1`：整文件 `#if !WINDOWS`（两文件互斥分部类）。
  - 伪命中提醒：`ImGuiRenderer.cs:54` 的 `#if 0` 在着色器 raw string 内，不是 C# 条件编译（见 9.1）。
- P/Invoke 一律 **`[LibraryImport]` + `internal static partial class`**（源生成器，全库 22 处：WPF/Maui/WinUI 的 `CloseHandle` × 3、Android `ANativeWindow`、Skia 的 `SKObjectiveC` libobjc × 3、示例 `CocoaHelper` × 3 等），配 `[MarshalAs(UnmanagedType.LPUTF8Str)]`、`nint`；COM 接口用 `[GeneratedComInterface]`（2 处：`ISwapChainPanelNative` 的 Maui/WinUI 双份）。**禁止手写 `[DllImport]`**（0 处）。
- 委托转函数指针：`Marshal.GetFunctionPointerForDelegate` + `GCHandle` 保活（Alloc/Free 在构造/Destroy 中配对）。
- 空指针实参**优先写 `default` 字面量**（不用 `null`）；仅当重载并存导致 `default` 无法推断类型时（典型场景：`ComPtr<T>` 与 `T*` 重载同时存在），写 `default(T*)` 显式限定，如 `default(ClearValue*)`、`default(ID3D12ProtectedResourceSession*)`（`sources/Zenith.NET.DirectX12/DXBuffer.cs:23-24`）、`default(IDXGIOutput*)`。唯一偏差是 `VKSwapChain.cs:170` 的字面 `null`（见 11.4、19.3）。COM 出参固定句式：

```csharp
context.Device.CreateXxx(&desc, SilkMarshal.GuidPtrOf<IDXxx>(), (void**)field.GetAddressOf()).Success();
```

---

## 17. 各子系统差异速查

| 维度 | 核心库 Zenith.NET（112 文件） | 三后端 | Extensions / Views | Experiments 示例 |
|---|---|---|---|---|
| 类型可见性 | public 抽象 API | 全 `internal`（仅 `Extensions` public） | 控件/扩展 public，实现 internal | **全 internal** |
| unsafe | 局部 `unsafe {}` 块 | **类级 `internal unsafe class`，粒度三后端不同**：DX 几乎全部（21/27 类文件）、VK 几乎全部（22/32，唯 `VKSampler` 例外）、Metal 仅 4 个碰指针的类 | 按需类级 | 碰指针即类级 |
| 主构造函数 | 几乎所有抽象类（唯 `GraphicsContext` 用普通构造函数） | 构造体为空/极简时用 | 属性初始化器捕获参数 | 仅个别（`Stroke`、`FluidSimulation` 等） |
| 基类引用 | — | 按需 `public new XxxGraphicsContext Context => (XxxGraphicsContext)base.Context;`（VK 15 / DX 7 / MTL 8 类） | — | — |
| Desc 暴露 | `public ref readonly XxxDesc Desc => ref desc;` | — | — | — |
| 接口 | **0 个** | 0 个 | 4 个声明 / 3 种（`IZenithView`、`IImGuiPlatformBindings`、`ISwapChainPanelNative`×2） | 0 个 |
| 事件 | **仅 1 个**（`GraphicsContext.ValidationMessage`） | 0 | `UpdateRequested`/`RenderRequested` | 消费方 |
| 注释 | **0 行（含 `//`）** | 仅分块标题 | 行注释仅 2 处、0 XML | 少量英文短句 |
| throw / try-catch | **0 / 0** | 能力检查 3 处 throw / 0 | **0 / FrameScheduler 1 处** | 5 处 throw / PlatformDetection 1 处 |
| 静态 App 上帝类 | 无 | 无 | 无 | 有（`static App()`） |
| 平台分支 | 无 `#if`、纯托管 | 无 `#if`，运行时分支 | 允许 `#if`（仅 3 文件，框架适配） | 运行时 `OperatingSystem.Is*()` |
| 互操作 | 无 P/Invoke | Silk.NET / Metal.NET 绑定 | LibraryImport/GeneratedComInterface | LibraryImport（CocoaHelper） |
| 代码复用 | 抽象复用 | Formats 集中 | **接受跨项目整文件复制**（平台自包含） | **接受示例间复制**（自包含） |
| `file struct` 常量缓冲 | — | — | 1 处（ImGuiRenderer） | 9 处（文件尾签名式模式） |

### 17.1 Views 层补充事实

- 六个 ZenithView 控件（`Zenith.NET.Views` 核心 + Avalonia / Maui / WPF / WinForms / WinUI）同构骨架：平台控件 + `IZenithView`、`XxxProperty` 静态只读依赖属性、`FrameScheduler`、`UpdateRequested`/`RenderRequested` 事件、显式接口实现垫底、`Loaded += async (_, _) => await scheduler.StartAsync();` 挂接（实证 `WPF/ZenithView.cs:28` 等 4 处逐字一致）。
- 设计时占位渲染：三平台使用**同一品牌渐变** `#512BD4 → #8A58FF → #00A4EF`（实证 `Avalonia/ZenithView.cs:50`、`WPF/ZenithView.cs:55`、`WinForms/ZenithView.cs:42`，均写作 `Color.FromRgb(0x51, 0x2B, 0xD4)` 形态）。
- `FrameScheduler` 有**重副作用静态构造函数**（`sources/Views/Zenith.NET.Views/FrameScheduler.cs:17`）：跑一次内存带宽基准测试得出性能分，再用 `double.Lerp` 决定帧间隔（`:43`）。
- WinUI 把显式接口实现拆到独立分部文件（`ZenithView.WinUI.cs` / `ZenithView.Uno.cs` 整文件 `#if` 互斥）。
- **逐字节级复制的文件对**：Maui-Windows 与 WinUI-Windows 的 `D3D.cs`/`Surface.cs`/`ISwapChainPanelNative.cs`（diff 仅差 using 头）；Maui iOS 与 MacCatalyst 的 `MauiZenithView.cs` 互为逐字副本。WPF 的 `D3D.cs`/`Surface.cs` 是同骨架改编版（D3D9Ex）。这是平台文件夹自包含原则下**接受的重复**（见 19.2）。

---

## 18. .editorconfig（建议配置）

**性质说明**：仓库当前**没有** `.editorconfig` 文件。零诊断靠"代码本身符合 VS 默认分析器 + `Directory.Build.props` 的 `EnforceCodeStyleInBuild` + Roslynator 分析器 + `.csproj` 的 `NoWarn`"维持。以下配置是**建议配置**：为让风格规则显式化、可执行，可在仓库根目录加入此 `.editorconfig`（与第 3~10 章逐条对应；未列出的 `dotnet_diagnostic` 默认沿用 VS 2026 内置严重级别，此处只锁定与默认偏好不同的项）：

```ini
# Zenith.NET 代码风格 —— 与 master@02d2fc8 全仓库实际风格一致
root = true

[*]
charset = utf-8-bom
end_of_line = lf
trim_trailing_whitespace = true
insert_final_newline = true

[*.cs]
indent_style = space
indent_size = 4

#### 大括号与换行（Allman） ####
csharp_new_line_before_open_brace = all
csharp_new_line_before_else = true
csharp_new_line_before_catch = true
csharp_new_line_before_finally = true
csharp_new_line_before_members_in_object_initializers = true
csharp_new_line_before_members_in_anonymous_types = true
csharp_new_line_between_query_expression_clauses = true

#### 缩进 ####
csharp_indent_case_contents = true
csharp_indent_switch_labels = true
csharp_indent_labels = no_change
csharp_indent_block_contents = true
csharp_indent_braces = false
csharp_indent_case_contents_when_block = true

#### var：全仓库禁用（0 处） ####
csharp_style_var_for_built_in_types = false:suggestion
csharp_style_var_when_type_is_apparent = false:suggestion
csharp_style_var_elsewhere = false:suggestion

#### 内建类型 ####
dotnet_style_predefined_type_for_locals_parameters_members = true:suggestion
dotnet_style_predefined_type_for_member_access = true:suggestion

#### 修饰符与可读性 ####
dotnet_style_require_accessibility_modifiers = for_non_interface_members:suggestion
dotnet_style_readonly_field = true:suggestion
csharp_preferred_modifier_order = public,private,protected,internal,file,static,extern,new,virtual,abstract,sealed,override,readonly,unsafe,required,volatile,async:suggestion

#### 模式匹配（优先 is/is not/switch 表达式） ####
csharp_style_prefer_pattern_matching = true:suggestion
csharp_style_prefer_pattern_matching_over_is_with_cast_check = true:suggestion
csharp_style_prefer_pattern_matching_over_as_with_null_check = true:suggestion
csharp_style_prefer_switch_expression = true:suggestion
csharp_style_prefer_not_pattern = true:suggestion
csharp_style_prefer_extended_property_pattern = true:suggestion

#### 表达式体：属性用、方法不用 ####
csharp_style_expression_bodied_methods = false:none
csharp_style_expression_bodied_constructors = false:none
csharp_style_expression_bodied_operators = false:none
csharp_style_expression_bodied_properties = true:suggestion
csharp_style_expression_bodied_indexers = true:suggestion
csharp_style_expression_bodied_accessors = true:suggestion
csharp_style_expression_bodied_lambdas = true:suggestion
csharp_style_expression_bodied_local_functions = false:none

#### 现代语法偏好（最新语法优先） ####
csharp_style_prefer_primary_constructors = true:suggestion
dotnet_style_prefer_collection_expression = when_types_loosely_match:suggestion
csharp_style_prefer_target_typed_new = true:suggestion
csharp_style_prefer_static_anonymous_function = true:suggestion
csharp_style_prefer_static_local_function = true:suggestion
csharp_style_prefer_file_local_namespace = true:suggestion
dotnet_style_prefer_auto_properties = true:suggestion
csharp_style_prefer_null_check_over_type_check = true:suggestion
csharp_style_prefer_local_over_anonymous_function = true:suggestion
csharp_style_prefer_tuple_swap = true:suggestion
csharp_style_prefer_utf8_string_literals = true:suggestion
dotnet_style_prefer_conditional_expression_over_assignment = true:suggestion
dotnet_style_prefer_conditional_expression_over_return = true:suggestion
dotnet_style_prefer_inferred_tuple_names = true:suggestion
dotnet_style_prefer_inferred_anonymous_type_member_names = true:suggestion
dotnet_style_prefer_is_null_check_over_reference_equality_method = true:suggestion
dotnet_style_prefer_simplified_boolean_expressions = true:suggestion
dotnet_style_prefer_compound_assignment = true:suggestion
dotnet_style_prefer_simplified_interpolation = true:suggestion
csharp_style_prefer_method_group_conversion = true:suggestion
csharp_style_prefer_top_level_statements = true:suggestion
csharp_style_prefer_range_operator = true:suggestion
csharp_style_implicitly_allowed_lambda_body_methods = true:suggestion
csharp_style_prefer_unbound_generic_type_in_nameof = true:suggestion
csharp_style_prefer_primary_constructors_in_structs = true:suggestion
csharp_prefer_system_threading_lock = true:suggestion
dotnet_style_object_initializer = true:suggestion
dotnet_style_collection_initializer = true:suggestion
dotnet_style_explicit_tuple_names = true:suggestion
dotnet_style_coalesce_expression = true:suggestion
dotnet_style_null_propagation = true:suggestion
dotnet_style_operator_placement_when_wrapping = beginning_of_line
csharp_style_prefer_index_operator = true:suggestion
csharp_style_namespace_declarations = file_scoped:suggestion
csharp_style_using_directive_placement = outside_namespace:suggestion

#### 空格 ####
csharp_space_after_cast = false
csharp_space_after_keywords_in_control_flow_statements = true
csharp_space_between_parentheses = false
csharp_space_before_colon_in_inheritance_clause = true
csharp_space_after_colon_in_inheritance_clause = true
csharp_space_around_binary_operators = before_and_after
csharp_space_between_method_declaration_parameter_list_parentheses = false
csharp_space_between_method_declaration_empty_parameter_list_parentheses = false
csharp_space_between_method_call_parameter_list_parentheses = false
csharp_space_between_method_call_empty_parameter_list_parentheses = false
csharp_space_before_comma = false
csharp_space_after_comma = true
csharp_space_before_semicolon_in_for_statement = false
csharp_space_after_semicolon_in_for_statement = true
csharp_space_around_declaration_statements = false
csharp_space_before_open_square_brackets = false
csharp_space_between_empty_square_brackets = false
csharp_space_between_square_brackets = false

#### using ####
dotnet_sort_system_directives_first = true
dotnet_separate_import_directive_groups = false

#### 命名规则：私有字段 camelCase 无前缀 ####
dotnet_naming_rule.private_fields_camel_case.severity = suggestion
dotnet_naming_rule.private_fields_camel_case.symbols = private_fields
dotnet_naming_rule.private_fields_camel_case.style = camel_case
dotnet_naming_symbols.private_fields.applicable_kinds = field
dotnet_naming_symbols.private_fields.applicable_accessibilities = private
dotnet_naming_style.camel_case.capitalization = camel_case

#### 与本仓库风格冲突、需显式关闭/降级的 IDE 规则 ####
dotnet_code_quality_unused_parameters = all:suggestion
dotnet_analyzer_diagnostic.category-Style.severity = default

#### 已知 NoWarn（与 .csproj 保持一致） ####
dotnet_diagnostic.IDE0130.severity = none
```

使用守则：

1. 该 `.editorconfig` 放仓库根目录即生效；**不得为了让分析器闭嘴而修改代码风格**——两者冲突时先核对本规范，以核心库现行写法为准再改配置。
2. 禁止在单个文件里用 `#pragma warning disable` 压制风格诊断（`ExtDescriptorHeap.cs` 这类仿生成文件除外）。
3. 提交前 VS 2026 的 Error List（含"信息"级）必须为空；CI（`.github/workflows/continuous-integration.yml`，Windows + macOS 双平台 `dotnet build -c Release`）必须零警告。

---

## 19. 规范适用范围与有意设计说明

### 19.1 不适用本规范的代码

以下文件**不是 Zenith.NET 的核心内容**，属于临时移植/仿生成代码，等待上游（Silk.NET 等）官方收录后即整体删除。它们刻意遵循其来源生态的风格，**不受本规范约束，也无需按本规范修改**：

| 文件 | 性质 |
|---|---|
| `sources/Zenith.NET.DirectX12/PipelineStateStream2.cs` | 移植的 D3D12 PSO 流布局代码（`_` 前缀私有字段、`[StructLayout(LayoutKind.Explicit)]` 显式布局） |
| `sources/Zenith.NET.Vulkan/ExtDescriptorHeap.cs`（2,722 行，占 Vulkan 项目 36%） | 仿 Silk.NET 生成风格手写的 `VK_EXT_descriptor_heap` 绑定（块作用域 `namespace Silk.NET.Vulkan` 与 `...Extensions.EXT`、`// <auto-generated/>`、`#pragma warning disable`、全库唯一的 `sealed`、XML 文档注释占位） |

### 19.2 有意设计（正确写法，勿"修正"）

| 位置 | 写法 | 为什么是对的 |
|---|---|---|
| 各 `Surface.cs` 等 | 空指针实参写 `default(T*)`（如 `default(IDXGIOutput*)`） | 当 `ComPtr<T>` 与 `T*` 等重载并存、`default` 字面量无法推断类型时，必须显式限定——这是 `default(T*)` 的正当使用场景（见 9、16 章） |
| `sources/Zenith.NET/GraphicsContext.cs:169` | `internal OnValidationMessage` 位于 abstract 列表之后 | 特例安排（见第 7 章） |
| `sources/Zenith.NET/CommandQueue.cs:71-77` | `protected abstract` 列表在 `Destroy()` 之前 | 特例安排（见第 7 章） |
| `sources/Zenith.NET/CommandBuffer.cs:447-473` | private `TryGetCurrentPipeline` 位于 `Destroy()` 与 abstract 块之间 | 特例安排（见第 7 章） |
| `sources/Zenith.NET.Metal/MTLCommandBuffer.cs:539` 起 | `Destroy()` 非最后，其后还有 private 编码器辅助方法与 4 个嵌套 struct | Metal 特例（见第 7 章） |
| `sources/Zenith.NET.Vulkan/VKFence.cs` | 继承 `DisposableObject`，用 `Context = context;` 普通属性赋值而非 `new Context` 隐藏属性 | VKFence 不属于 RHI API 体系，只能继承 `DisposableObject` |
| `VKSampler.cs`、`DXShader.cs`、`DXGraphicsPipeline.cs` 等 | `GetNativeObject` 直接 `return 0;` | 该类型没有任何需要暴露给外部的原生对象时，直接 `return 0;` 是正常行为，无需统一为 switch 表达式（两种合法形态，见 11.1） |
| `sources/Zenith.NET.Vulkan/VKSampler.cs:11` | 带可选参数构造函数的 struct 写 `new SamplerCreateInfo() { ... }` 全类型名 | `new()` 在此处语义不同，保留全类型名是有意的（见 11.5） |
| `DXGraphicsPipeline` / `DXMeshShadingPipeline`、`VKGraphicsPipeline` / `VKMeshShadingPipeline` | 大段逐字重复（`// AttachmentFormats`、`// RenderState` 块） | 刻意未抽公共方法，属接受的重复 |
| Maui-Windows 与 WinUI-Windows | `D3D.cs`/`Surface.cs`/`ISwapChainPanelNative.cs` 逐字节级双份（diff 仅差 using 头）；Maui iOS/MacCatalyst 的 `MauiZenithView.cs` 逐字双份 | 平台文件夹自包含原则下接受的重复 |
| 核心库资源类（Buffer 等 11 类） | `private BufferDesc desc = desc;` 主构造参数同名遮蔽 | 规范写法（见 6.1），非笔误 |

### 19.3 演进中的细节差异

| 位置 | 现象 | 取向 |
|---|---|---|
| CornellBox `CameraHandler` vs FluidTank | CornellBox 同文件内混用 `arg1/arg2/arg3` 与 `mouse`/`vector`（实证 `sources/Experiments/CornellBox/Handlers/CameraHandler.cs:89-97`）；FluidTank 已全语义化（`mouse`/`button`/`scanCode`/`position`） | 新代码一律用语义名 |
| CornellBox `ImGuiHandler` vs FluidTank 版 | FluidTank 版用区间关系模式折叠键位映射（`>= Key.Number0 and <= Key.Number9 => ...`，`FluidTank/Handlers/ImGuiHandler.cs:140`），是"打磨版" | 新代码参照 FluidTank 版 |
| `sources/Experiments/InkCanvas/Drawing/Stroke.cs` | 全库唯一无 BOM 的文件 | 新文件一律 UTF-8 带 BOM |
| `Experiments/FluidTank/Program.cs`、`Experiments/InkCanvas/Program.cs`、`Experiments/FluidTank/Helpers/CocoaHelper.cs`、`Experiments/InkCanvas/Helpers/CocoaHelper.cs` | 4 个文件无末尾换行 | 新文件一律以单个末尾换行收尾 |
| `sources/Zenith.NET.Vulkan/VKSwapChain.cs:170` | 空指针实参用字面 `null`（与"优先 `default`"的唯一偏差） | 新代码一律写 `default` |

---

## 附录 A：快速自检清单（提交前）

- [ ] 文件 UTF-8 BOM + LF + 4 空格 + 无行尾空白 + 末尾换行
- [ ] 文件作用域 namespace；一文件一类型；文件名 = 类型名
- [ ] 无 `var`；所有可推断处用了 `new()` / `new(...)`
- [ ] 不捕获闭包的 lambda 都加了 `static`
- [ ] 常量比较全部写成 `is` / `is not` 模式；映射用 switch 表达式 + `_ => default`
- [ ] 集合初始化用 `[]` / `[.. ]`
- [ ] Allman 大括号；成员间单空行；方法内空行分组
- [ ] 长行单行优先；换行处参数与首参数列对齐
- [ ] 私有字段 camelCase 无前缀；internal 类需要外露的成员用 public 字段
- [ ] 成员顺序：字段 → 构造 →（按需）`new Context` 收窄 → 属性 → 方法 → `Destroy()`/抽象 `Impl` 收尾
- [ ] 后端类按指针需要标类级 `unsafe`（对齐所在后端粒度：DX/VK 默认标，Metal 仅碰指针才标）
- [ ] 资源类只重写 `Destroy()`；临时资源用 using 声明；借用的 `CommandBuffer`/`Drawable` 不 Dispose
- [ ] 无参数校验、无 Guard、无 Assert、无防御性 try/catch
- [ ] 无 XML 文档注释；注释仅英文短句分块标题或"为什么"说明
- [ ] 空指针实参写 `default`（重载歧义时才 `default(T*)`）
- [ ] VS 2026 Error List 零错误/零警告/零信息；CI 双平台 `dotnet build -c Release` 零警告

---

## 附录 B：权威统计（master @ 02d2fc8）

以下数字来自全量脚本扫描（256 个 `.cs` / 26,783 行），为本规范的统计基准：

| 指标 | 精确值 |
|---|---|
| C# 文件数 / 总行数 | 256 / 26,783 |
| UTF-8 BOM | 255/256（唯一例外 `Experiments/InkCanvas/Drawing/Stroke.cs`） |
| LF 换行 | 256/256 |
| Tab 缩进文件 / 行尾空白 | 0 / 0 |
| 文件作用域 namespace | 248；无 namespace 7 个（4 个 Program.cs 顶层语句 + 3 个后端 Usings.cs）；块作用域 1 个（`ExtDescriptorHeap.cs`，含 `Silk.NET.Vulkan` 与 `Silk.NET.Vulkan.Extensions.EXT` 两个命名空间）。合计校验：248 + 7 + 1 = 256 |
| target-typed `new()` | ≈706 |
| `var` | 0 |
| `record` / `sealed` | 0 / 1（唯一 sealed 在 `ExtDescriptorHeap.cs` 的 `GeneratedVTable`，属仿生成代码） |
| `ArgumentNullException` / `Debug.Assert` / `Guard.` / `[DllImport]` | 0 / 0 / 0 / 0 |
| `is` 模式 | ≈364（其中 `is not` 185） |
| switch 表达式 | ≈120+ |
| `.Success()` | 全库 143：Vulkan 83、DirectX12 52、Metal 8 |
| static lambda | 16（核心 Uploader/Downloader、VKCapabilities/VKGraphicsContext/VKGraphicsPipeline/VKMeshShadingPipeline、Maui Extensions、ImGuiRenderer `Comparer.Create`、3 个示例 App 的 ValidationMessage） |
| extension 块 | 71 处（三后端 Extensions.cs + ExtDescriptorHeap.cs + ImageSharp/Skia/Maui Extensions） |
| 主构造函数类型 | ≈48 |
| `field` 关键字 | 4 个文件：`GraphicsResource.cs`（Name setter 校验）、`DXGraphicsContext.cs`（4 个 `field ??=` 懒加载堆）、`VKGraphicsContext.cs`（`field ??=` ResourceHeap）、`FluidSimulation.cs`（`set => field = Math.Clamp(...)`） |
| `[LibraryImport]` / `[GeneratedComInterface]` | 22 / 2（WPF/Maui/WinUI CloseHandle×3、Android ANativeWindow、Skia SKObjectiveC libobjc×3、示例 CocoaHelper×3 等；COM 接口为 ISwapChainPanelNative 双份） |
| 接口 | 4 个声明 = 3 种：`IZenithView`、`IImGuiPlatformBindings`、`ISwapChainPanelNative`（Maui/WinUI 各一份逐字副本）；核心库 0 接口 |
| throw | 全库 8 处：`NotSupportedException`×4（DX/MTL/VK GraphicsContext 能力检查 + PlatformDetection switch 臂）、`PlatformNotSupportedException`×4（3 个示例 App 平台检查 + FluidTank X11 检查） |
| try/catch | 2 个文件：`PlatformDetection/Program.cs`（`catch (Exception)` 探测式吸收）、`Views/FrameScheduler.cs`（`catch (OperationCanceledException) { break; }` + 裸 catch 保活循环） |
| C# 级 `#if` | 3 文件：`Maui/ZenithViewHandler.cs`（选 using）、`WinUI/ZenithView.WinUI.cs`（整文件 `#if WINDOWS`）、`WinUI/ZenithView.Uno.cs`（`#if !WINDOWS`）。伪命中：`ImGuiRenderer.cs:54` 的 `#if 0` 在着色器 raw string 内 |
| XML 文档注释 | 仅 2 文件：`DirectX12/PixHelpers.cs`（移植出处 URL）、`Vulkan/ExtDescriptorHeap.cs`（仿生成占位） |
| `file` 类型 | 10 处（9 个示例 file struct 常量缓冲 + `ImGuiRenderer.cs` 文件尾 `file struct Constants`） |
| 核心库（112 文件） | 0 var / 0 record / 0 sealed / 0 interface / 0 注释（含 `//`）/ 0 throw / 0 try-catch；23 个 abstract class；事件仅 1 个（`ValidationMessage`）；using 仅 8 个文件有 |
| `params ReadOnlySpan<T>` | 1 处（`CommandBuffer.Submit`） |
| 无末尾换行的文件 | 4 个（FluidTank/InkCanvas 的 Program.cs 与 Helpers/CocoaHelper.cs） |
| 各子系统行数 | Vulkan 7,579（其中 `ExtDescriptorHeap.cs` 2,722 占 36%）/ Experiments 4,677 / 核心 4,408 / DX12 4,087 / Metal 2,812 / Views 1,957 / Extensions 1,263 |
