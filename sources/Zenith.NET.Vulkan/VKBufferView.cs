namespace Zenith.NET;

internal class VKBufferView(GraphicsContext context, BufferViewDesc desc) : BufferView(context, desc)
{
    protected override void SetResourceName(string name)
    {
    }

    protected override void Destroy()
    {
    }
}
